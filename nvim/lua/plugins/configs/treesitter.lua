local options = {
  ensure_installed = { "lua", "rust", "python", "go", "typescript", "javascript" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
